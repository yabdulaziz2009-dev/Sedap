import { createSlice } from "@reduxjs/toolkit";


const initialState = {
  value: 0,
}




export const statrSlice = createSlice({
  name: 'start',
  initialState,
  reducers: {

  },
})

export default statrSlice.reducer